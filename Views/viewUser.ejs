<%-include('header')%>

          <!-- Content wrapper -->
          <div class="content-wrapper">
            <!-- Content -->

            <div class="container-xxl flex-grow-1 container-p-y">
              <!-- Basic Bootstrap Table -->
              <div class="card">
                <h5 class="card-header">View Plan List</h5>
                  <% if(flash.success && flash.success.length> 0 ) { %>
                    <div class="alert alert-success">
                         <%= flash.success %>
                    </div>
                    <% } %>
                  <table class="table">
                    <thead>
                      <tr>
                        <th>Movie</th>
                        <th>Mall</th>
                        <th>Show Time</th>
                        <th>Phone no</th>
                        <th>Email</th>
                        <th>No of seat</th>
                        <th>Pay Ruppe</th>
                        <th>Action</th>
                      </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                         <%for(var i of user){for(var o of plan){%>
                      <tr>
                        <%if(i.planId.id == o.id){%>
                        <td><%=o.movieId.title%></td>
                        <td><%=o.mallId.mall%></td>
                        <td><%=o.show_time%></td>
                        <td><%=i.phone%></td>
                        <td><%=i.email%></td>
                        <td><%=i.sheets%></td>
                        <td><%=i.total%> ₹</td>
                        <td>
                          <a href="/deleteUser/<%=i.id%>" class="btn btn-danger">Delete</a>
                         </td>
                        <%}%>
                      </tr>
                    </tbody>
                    <%}}%>
                  </table>
              </div>
              <br>
              <br>
<%-include('footer')%>